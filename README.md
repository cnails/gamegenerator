# Game Generator

Game Generator — конвейер быстрого прототипирования вертикальных мобильных игр на TypeScript + Phaser 3 с глубокой интеграцией ChatGPT API. Один веб-интерфейс позволяет собрать четыре жанра (платформер, аркада, головоломка, башенная оборона), автоматически адаптировать сцену под соотношение 9:16 и наполнить механику контентом, подсказанным LLM.

## Возможности

- 🎮 **Готовые жанровые шаблоны**: `PlatformerScene`, `ArcadeScene`, `PuzzleScene`, `TowerDefenseScene`
- 📱 **Вертикальный лайаут 9:16**: `VerticalBaseScene` рассчитывает safe/play bounds, HUD и pointer-ввод
- 🎯 **Унифицированный scoring**: `BaseGameScene` ведёт очки, HUD, завершение раунда и оверлеи
- 🔄 **Game Swiper**: `GameContainer` даёт плавные свайпы между играми без выхода в меню
- 🤖 **ChatGPT API для контента**: JSON-конфигурации врагов, башен, волн, сеток match-3, SVG-спрайты
- 🧱 **Генератор ассетов**: LLM присылает 16-битные SVG с планом анимаций, которые загружаются на лету
- 💾 **LocalStorage и перезапуск**: сгенерированные игры и параметры сохраняются в браузере

## Технологии

- **TypeScript** (strict, ESM)
- **Phaser 3** — игровой движок и физика
- **Vite** — dev-сервер и продакшн-сборка
- **ChatGPT API** — интеграция в `src/api`
- **LocalStorage** — `src/storage`

---

## 1. Анализ аудитории и продукта

Проект обслуживает две основные роли:

1. **Создатели игр** (студенты, джуны, команды на хакатонах, внутренние разработчики). Их задачи:
   - собрать рабочий прототип за часы, а не недели;
   - мгновенно переключаться между жанрами (платформер, аркада, match‑3, tower defense);
   - делегировать генерацию врагов, башен, блоков и даже графики нейросети.  
     В веб-интерфейсе пользователь выбирает шаблон из `src/templates/index.ts`, настраивает параметры (`speed`, `enemySpawnRate`, `waveCount`, `gridSize` и т.д.), добавляет промпт, и `GameManager` комбинирует сцену, механику и ассеты.

2. **Конечные игроки**, которые запускают уже сгенерированные игры на смартфоне:
   - портретный режим без артефактов, soft-safe-зоны, корректная работа жестов;
   - понятные правила «с первой секунды»: в платформере прыгай и избегай шипов, в аркаде уворачивайся и стреляй, в match‑3 собирай комбинации, в башенной обороне ставь башни на слот.

Game Generator — это не одна игра, а фабрика игр. Конкретная конфигурация хранится в `GeneratedGame` (тип из `src/types`), сериализуется в localStorage и может быть восстановлена через `BaseGameScene`.

---

## Game Swiper

`src/core/GameContainer.ts` — обёртка над `GameManager`, которая загружает/выгружает Phaser-сцены и даёт TikTok-стиль перелистывания. Основной жест — **двумя пальцами вертикально по центру**, чтобы управление внутри игры оставалось незатронутым; для UX добавлены ненавязчивые стрелки-подсказки и очередь переходов, чтобы не терять жесты подряд.

### Как добавить/запустить игру в свайпере
1. Создайте игру через UI — `GameStorage.saveGame` добавит её в localStorage.
2. Событие `playGame` передаст массив `GeneratedGame[]` в `GameContainer.setGames(games, selectedId)` и откроет нужный индекс.
3. Для быстрого расширения просто добавьте игру в массив перед `setGames` — контейнер возьмёт её в список и позволит перелистывать через `navigate(±1)`.

### Настройка жестов
- `horizontalEdgeSize`/`verticalEdgeSize` и `horizontalSwipeThreshold`/`verticalSwipeThreshold` задаются при создании `GameContainer`. По умолчанию TikTok-жест срабатывает, когда **двумя пальцами** (pointerType `touch`) сдвинули экран >120 px.
- Центральная зона (`.game-stage`) считает только два активных касания: если оба остаются на экране и вертикальный вектор доминирует, запускается переход; иначе события остаются в игре.
- Горизонтальные зоны выключены (можно удалить из DOM). Верх/низ всё ещё можно использовать как визуальные «ручки»; ширина задаётся `verticalEdgeSize`.
- Анимация задаётся в CSS (`.game-stage.swipe-transition`, `.swipe-up/down`). Хотите fade или slide-in/out — замените трансформации и длительность `transitionDuration` в `GameContainer`.

## Мобильное управление платформера

`src/core/input/InputController.ts` отвечает за touch-жесты в `PlatformerScene`. Он подписывается на `pointerdown/move/up` и переводит жесты в команды:

- свайп/удержание влево — движение налево;
- свайп/удержание вправо — движение направо;
- свайп вверх или короткий тап — прыжок (запрос хранится до ближайшего касания земли);
- на десктопе по-прежнему работают клавиши ←/→ и Space/↑.

### Как добавить новый жест
1. Расширьте `InputController` или доработайте `handlePointerMove/handlePointerUp`.
2. Для прыжка вызывайте `queueJump()`, для движения меняйте `state.direction`.
3. Подключите контроллер в `PlatformerScene.initializeInputController()` — сцена автоматически подхватит новые жесты.

### Настройка чувствительности и физики
- Пороги свайпа/тапа задаются в `movementSettings` (поля `swipeThreshold`, `tapThreshold`, `holdDeadzone`) и передаются в контроллер.
- Высоту прыжка регулирует `movementSettings.jumpVelocity` и метод `performJump()`.
- Плавность горизонтального движения настраивается с помощью `groundControl`/`airControl`: увеличьте значения для резкого отклика или уменьшите для более мягкого ускорения.

### UI и анимации
- Все стили находятся в `src/styles/main.css` (классы `.game-wrapper`, `.edge-swipe-zone`, `.game-swiper-overlay`, `.game-thumb`).
- Переход реализован через `game-transition-layer` (fade). Можно заменить на slide, подменив переходы в CSS.
- Мини-карточки (`.game-thumb`) позволяют кликнуть по конкретной игре, а кнопка «← Выйти в меню» возвращает к `MainScreen`.

---

## 2. Вертикальный шаблон и сцены (дизайн-док)

`src/scenes/templates/VerticalStandardScene.ts` содержит класс `VerticalBaseScene`, который наследуется вместо `BaseGameScene` во всех реальных сценах.

### Быстрый старт для новой сцены

1. Наследуйтесь от `VerticalBaseScene` вместо прямого `BaseGameScene`.
2. В `initGame()` первым делом вызовите `this.initVerticalLayout({ ...options })`.
3. Создавайте механику: фон, физику, HUD, используя `safeBounds`/`playBounds` для позиционирования.
4. Для тач-управления включите `enablePointer` и переопределите `onPointerDown/Move/Up`.
5. В `onSafeAreaChanged()`/`update()` реагируйте на изменение экрана (перемещайте HUD, пересчитывайте камеру).
6. При кастомном завершении сцены до `SHUTDOWN` вызовите `this.destroyVerticalLayout()`, чтобы снять обработчики resize/pointer (иначе это сделает базовый класс автоматически).

### Ключевые обязанности и API `VerticalBaseScene`

- **initVerticalLayout(options)** — «магическая кнопка». Рассчитывает:
  - `safeBounds`: прямоугольник, вписанный в экран с учётом соотношения 9:16, min/max ширины и отступов;
  - `playBounds`: игровое поле внутри `safeBounds`;
  - подписки на события `resize` и pointer-ввод (`extraPointers`, multi-touch).  
- **HUD-хуки**:
  - `applyHudLayout()` выравнивает `scoreText` и прочие элементы по `safeBounds`;
  - `onSafeAreaChanged(safe, play)` доступен дочерним классам (`ArcadeScene`, `PlatformerScene` и т.д.) для перестройки интерфейса при изменении размеров.
- **Pointer-хуки**: `onPointerDown/Move/Up` заменяют ручные подписки в сценах — достаточно включить `enablePointer`.
- **Помощники**: `clampXWithinSafeArea`, `clampYWithinSafeArea`, `getSafeBounds`, `getPlayableBounds`.

| Метод / свойство | Назначение |
| --- | --- |
| `initVerticalLayout({ targetAspect, minSafeWidth, maxSafeWidth, paddingX, paddingY, enablePointer, extraPointers })` | Настройка портретного поля и подключение listeners. |
| `safeBounds` | Безопасная область размещения HUD/управления; автоматически пересчитывается при `resize`. |
| `playBounds` | Внутренняя область для игрового мира с учётом padding. |
| `onSafeAreaChanged(safe, play)` | Хук для динамического перелэйаута (камера, HUD, фон). |
| `onPointerDown/Move/Up` | Унифицированный обработчик pointer-ввода, активируется при `enablePointer: true`. |
| `clampXWithinSafeArea`, `clampYWithinSafeArea` | Быстрый кламп координат внутри safe-area. |
| `destroyVerticalLayout()` | Снимает обработчики resize/pointer; вызывается автоматически на `SHUTDOWN`, но доступен вручную. |

### Как сцены используют шаблон

```ts
export class MyScene extends VerticalBaseScene {
  initGame(): void {
    this.initVerticalLayout({ enablePointer: true, extraPointers: 1 });
    // ... создание мира, HUD, механики
  }

  protected onPointerDown(pointer: Phaser.Input.Pointer): void {
    // прыжок, выстрел, перемещение башни
  }

  protected onSafeAreaChanged(): void {
    // пересчитать позиции HUD, камеры, кнопок
  }
}
```

- **ArcadeScene**: корабль игрока, HUD и фон выравниваются по `safeBounds`; при смене ориентации `onSafeAreaChanged` передвигает элементы. Pointer-ввод позволяет вести корабль пальцем.
- **PlatformerScene**: safe-area определяет видимый сегмент вертикального уровня, камера прикрепляется к игроку с оффсетами; HUD и виртуальные кнопки всегда остаются на безопасных координатах.
- **PuzzleScene**: сетка, progress-bar и текстовые панели занимают 80% высоты `safeBounds`, что делает match‑3 аккуратным на разных телефонах.

Таким образом, Vertical 9:16 — это не просто «абстракция в коде», а UX-решение, гарантирующее визуальную целостность на любом устройстве. Всегда базируйте HUD и кнопки на координатах `safeBounds`, чтобы избежать ручной обработки `resize`.

**Дополнительные материалы:** полный reference по вертикальному шаблону доступен в [docs/vertical-template.md](https://raw.githubusercontent.com/cnails/gamegenerator/main/docs/vertical-template.md).

---

## 3. Жанры и правила (краткая документация)

`src/templates/index.ts` описывает параметры каждого жанра, а конкретная логика живёт в `src/scenes/templates`.

### Платформер (`PlatformerScene.ts`)

- Вертикальный раннер: камера следует за игроком вверх, уровень выше экрана.
- Таймер (по умолчанию 120 секунд), комбо-система (`comboMultiplier` с затухающим таймером), бонусы (`speed`, `shield`, `scoreBoost`).
- Опасности: статические шипы (`hazards`), падающие препятствия (`spawnFallingObstacles`) и патрулирующие враги с разными архетипами.
- Управление: клавиатура или виртуальные кнопки (`setupMobileControls`).
- Настройка через `PlatformerVariantSettings`: цели `collect/score/survive`, количество монет, параметры врагов/опасностей, собственные палитры.

### Аркада (`ArcadeScene.ts`)

- Вертикальный шутер: корабль игрока, волны врагов (`waves`), усиления (`powerUps`), комбо (`comboLabel`).
- Pointer-ввод позволяет вести корабль пальцем, multi-touch поддерживается через `extraPointers`.
- Враги описываются `ArcadeEnemyProfile`, волны — `ArcadeWaveDefinition`, цели — `ArcadeObjective`.
- Scoring: за уничтожение врагов + комбо множитель, бонус за выполнение цели, штраф при получении урона.

### Головоломка (`PuzzleScene.ts`)

- Match‑3 сетка от 4×4 до 9×9, параметризуемая через `puzzleVariant`.
- LLM присылает типы блоков (`PuzzleBlockType`), бонусные правила (`PuzzleBonusRule`), заблокированные клетки (`PuzzleBoardModifier`).
- Система ходов (`movesLeft`), целей (`targetMatches`), прогресс-бар и бонусы за каскады.
- Спец-блоки (`bomb`, `lineHorizontal`, `colorClear`, `scoreBoost`) активируются в `applySpecialBlockEffects`.

### Башенная оборона (`TowerDefenseScene.ts`)

- Синусоидальная дорожка (`createPath`), tower slots вокруг пути, авторазмещение башен.
- LLM формирует `towerTypes`, `enemyTypes`, `waves`. Параметры пользователя: `waveCount`, `towerSlots`, `towerVariety`.
- `BaseGameScene` HUD показывает здоровье базы, волну, энергию (credits).  
- Враги двигаются по путевым точкам, башни автоматически выбирают цели в радиусе. Победа после отбивания всех волн, поражение при уроне базе.

### Scoring и сохранение

- `BaseGameScene` управляет очками (`updateScore`), HUD, кнопкой выхода, оверлеями победы/поражения.
- Все шаблоны могут сохранять конфигурацию и прогресс в localStorage, чтобы перезайти в игру позже.

---

## 4. Процесс генерации и работа с ИИ

Используем LLM на двух уровнях:

1. **В разработке** — ChatGPT помогал разложить задачу и предложил архитектурные решения:
   - вынесение вертикального лайаута в `VerticalBaseScene` и хуки `onSafeAreaChanged`, `initVerticalLayout`;
   - единый API для pointer-событий и safe-area;
   - идея `puzzleVariant` как самостоятельного JSON-объекта (блоки, бонусы, заблокированные клетки);
   - параметрические башни и враги вместо жёстко закодированных классов.

2. **В рантайме** — ChatGPT API генерирует контент:
   - **Match‑3**: промпт вида «Создай puzzleVariant 8×8 с 5 типами блоков, двумя бонусами и заблокированными клетками» → JSON парсится в `PuzzleVariantSettings`.
   - **Tower Defense**: запрос «4 вида башен (быстрая/мощная/замедляющая/AoE) и 4 вида врагов» → JSON в `towerTypes` и `enemyTypes`.
   - **Аркада**: «Сгенерируй 10 волн с тремя типами врагов» → `ArcadeVariantSettings`.
   - **SVG-ассеты**: промпт «16-битный герой-рыцарь» → SVG + `SpritePlanEntry`, загружается через `BaseGameScene.prepareLlmSprites()` и используется в сценах (`getLlmTextureKey`, `fitSpriteToLlmMeta`).

API-ключ вводится пользователем в UI (поле «API ключ ChatGPT»). Если ключ не указан, игра использует дефолтные наборы параметров из `src/templates`.

---

## 5. Мини-геймдизайн-документ

| Жанр | Игровой цикл | Key UX/HUD элементы | Система очков |
|------|--------------|---------------------|---------------|
| Платформер | Игрок стартует снизу и поднимается вверх, уклоняясь от шипов и врагов. 1–2 минуты на прохождение. | Таймер, комбо, цели, виртуальные кнопки в `safeBounds`. | Очки за сбор кристаллов и скорость, бонус при выполнении цели. |
| Аркада | Поток врагов сверху вниз, игрок маневрирует кораблём и собирает бусты. | HUD: здоровье, таймер/цель, название волны, комбо. | Очки за каждого врага, множитель за серию, бонус за выживание. |
| Match‑3 | Ограничение по ходам, цель по совпадениям. Спец-блоки добавляют тактику. | Instruction text, progress bar, ходы, комбо, бонусные уведомления. | Очки за очищенные блоки, каскады, бонусы LLM (extra moves, score). |
| Tower Defense | Враги идут по пути, башни стреляют автоматически. Игрок управляет только конфигурацией (через генерацию). | HUD: здоровье базы, номер волны, энергия. | Очки за каждого врага + финальный бонус за здоровье базы. |

Расширение продукта:

- Добавление нового жанра = новый класс сцены, наследующий `VerticalBaseScene`, плюс описание шаблона в `src/templates/index.ts` и новый тип промпта для LLM.
- Safe-area, pointer-ввод, HUD-хуки уже решены, поэтому новые жанры органично впишутся в мобильный UX.

---

## Установка

```bash
npm install
```

## Запуск

```bash
# Режим разработки
npm run dev

# Сборка
npm run build

# Предпросмотр
npm run preview
```

## Использование

1. Запустите dev-сервер и откройте `http://localhost:3000`.
2. Выберите шаблон (платформер, аркада, головоломка, башенная оборона).
3. Настройте параметры (скорость, количество волн, размер сетки и т.д.).
4. (Опционально) Вставьте API-ключ ChatGPT и свой промпт для генерации варианта.
5. Нажмите «Сгенерировать игру» — конфигурация сохранится в localStorage, можно вернуться позже.

## Структура проекта

```
gamegenerator/
├── src/
│   ├── api/           # ChatGPT API интеграция
│   ├── core/          # GameManager, конфигуратор
│   ├── scenes/
│   │   ├── BaseGameScene.ts
│   │   └── templates/ # ArcadeScene, PlatformerScene, PuzzleScene, TowerDefenseScene, VerticalStandardScene
│   ├── templates/     # Описание жанров и параметров UI
│   ├── storage/       # Работа с localStorage
│   ├── ui/            # Компоненты интерфейса
│   └── main.ts        # Точка входа
├── index.html
├── package.json
├── tsconfig.json
└── vite.config.ts
```

## Добавление нового шаблона

1. Создайте сцену в `src/scenes/templates/`, наследуя `VerticalBaseScene` или `BaseGameScene`.
2. Опишите параметры в `src/templates/index.ts`.
3. Зарегистрируйте сцену в `GameManager.getSceneClassForTemplate()`.
4. (Опционально) Добавьте промпт в UI и схему ответа для ChatGPT.

```ts
// src/scenes/templates/MyGameScene.ts
import { VerticalBaseScene } from './VerticalStandardScene';

export class MyGameScene extends VerticalBaseScene {
  protected initGame(): void {
    this.initVerticalLayout({ enablePointer: true });
    // игровая логика
  }
}
```

## ChatGPT API

1. Получите ключ на [platform.openai.com](https://platform.openai.com).
2. Введите его в UI (поле «API ключ ChatGPT») перед генерацией.
3. Настройте промпт для нужного жанра (вариант головоломки, башен, волн и т.д.).
4. Без ключа будут использованы дефолтные пресеты.

## Лицензия

MIT
