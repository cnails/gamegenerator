# Game Generator

Приложение для генерации простых 2D-игр для мобильных устройств с использованием TypeScript, Phaser 3 и ChatGPT API.

## Возможности

- 🎮 **Четыре шаблона игр**: Платформер, Аркада, Головоломка, Башенная оборона
- 🎯 **Система наград и очков**: Зарабатывайте очки и награды в каждой игре
- 🤖 **Интеграция с ChatGPT API**: Генерация уникальных игр на основе шаблонов
- 📱 **Оптимизация для мобильных**: Портретная ориентация, адаптивный интерфейс
- 💾 **Локальное хранилище**: Все игры сохраняются в localStorage

## Технологии

- **TypeScript** (strict mode)
- **Phaser 3** - игровой движок
- **Vite** - сборщик и dev-сервер
- **ESM** - модульная система

## Установка

```bash
npm install
```

## Запуск

```bash
# Режим разработки
npm run dev

# Сборка для продакшена
npm run build

# Просмотр собранной версии
npm run preview
```

## Использование

1. Откройте приложение в браузере (по умолчанию `http://localhost:3000`)
2. Выберите шаблон игры (Платформер, Аркада или Головоломка)
3. Настройте сложность и параметры
4. (Опционально) Укажите API ключ ChatGPT для генерации уникальных игр
5. Нажмите "Сгенерировать игру"
6. Играйте и зарабатывайте очки!

## Шаблоны игр

### Платформер
- Вертикальный платформер под портретные экраны: камера следует за игроком, мир растянут по высоте
- Таймер на 2 минуты, комбо-множитель и бонусы за серию сборов
- Новые опасности: шипы, падающие препятствия и патрулирующие враги
- Сенсорные кнопки движения/прыжка для мобильных устройств
- Параметры: скорость, количество врагов, длительность уровня

### Аркада
- Вертикальный мобильный шутер с безопасной зоной под смартфоны
- Волны разных типов врагов (маневрирующие, бронированные, стреляющие)
- Усиления: щит, тройной выстрел, быстрая стрельба и комбо-множитель
- Таймер раунда, шкала жизней и вибрация/шейк при получении урона
- Параметры: скорость, частота появления врагов, длительность раунда

### Головоломка
- Собирайте 3+ одинаковых блока подряд
- Параметры: размер сетки, целевое количество совпадений

### Башенная оборона (Tower Defence)
- Извилистая дорожка с волнами врагов, идущих к вашей базе
- LLM генерирует уникальные виды башен с эффектами и набор разнообразных врагов
- Параметры: количество волн, число позиций под башни, разнообразие видов башен

## ChatGPT API

Для использования генерации игр через ChatGPT API:

1. Получите API ключ на [platform.openai.com](https://platform.openai.com)
2. Введите ключ в поле "API ключ ChatGPT" при создании игры
3. Если ключ не указан, будут использованы значения по умолчанию

## Структура проекта

```
gamegenerator/
├── src/
│   ├── api/           # API интеграции (ChatGPT)
│   ├── core/          # Основная логика (GameManager)
│   ├── scenes/        # Игровые сцены Phaser
│   │   ├── templates/ # Шаблоны игр
│   │   └── BaseGameScene.ts
│   ├── storage/       # Локальное хранилище
│   ├── styles/        # CSS стили
│   ├── templates/     # Определения шаблонов
│   ├── types/         # TypeScript типы
│   ├── ui/            # UI компоненты
│   └── main.ts        # Точка входа
├── index.html
├── package.json
├── tsconfig.json
└── vite.config.ts
```

## Добавление новых шаблонов

1. Создайте новый класс сцены в `src/scenes/templates/`, наследуя от `BaseGameScene`
2. Добавьте определение шаблона в `src/templates/index.ts`
3. Зарегистрируйте шаблон в `GameManager.getSceneClassForTemplate()`

Пример:

```typescript
// src/scenes/templates/MyGameScene.ts
import { BaseGameScene } from '../BaseGameScene';

export class MyGameScene extends BaseGameScene {
  protected initGame(): void {
    // Ваша игровая логика
  }
}
```

## Лицензия

MIT
